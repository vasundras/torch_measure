```json
{
  "reviewer": "testing",
  "findings": [
    {
      "id": "CRIT-TEST-1",
      "title": "submission/train.py has zero test coverage (458 LoC trainer producing 3 load-bearing artifacts is untested)",
      "severity": "P1",
      "confidence": 95,
      "category": "untested-branches-in-new-code",
      "anchor": {
        "file": "/Users/ankit.aggarwal/Dropbox/Stanford/CS321M/predictive-eval-competition/vasundras_torch_measure/submission/train.py",
        "lines": "1-458"
      },
      "evidence": "Verified by directory listing + grep: tests/test_submission/ contains only test_caimira_lite.py and test_model_contract.py; `find tests -name 'test_train*'` returns empty. Yet train.py is 458 LoC and produces the THREE artifacts that submission/model.py loads at module init (caimira_lite.pt, caimira_lite.meta.json, eb_tables.json — model.py:86-88). The trainer contains substantive logic with multiple non-trivial branches: binary-benchmark filter (train.py:182-185 — `is_binary = bool(labels) and all(v in (0.0, 1.0) for v in labels)`), Bayesian shrinkage at EB level 2 (train.py:271-288), state_dict round-trip verification (train.py:387-403), the `_build_wide_response` long-to-wide conversion (train.py:444-454), the `_table_clip`/`_logit`/`_sigmoid` math (train.py:226-239), and the `_EB_SBC_MIN_N=3` / `_EB_SUBJ_MIN_N=10` / `_EB_SB_ALPHA=5.0` hyperparameters (train.py:72-74). The whole `build_eb_tables` function (train.py:242-308) and the `--smoke` subset selection (train.py:159-160, 320-323, 346) are untested. A bug in any of these would silently ship into the produced artifacts and only surface as a Codabench hidden-leaderboard regression — the exact failure mode the parent CLAUDE.md \"2026-05-22 transfer postmortem\" warns against.",
      "why_it_matters": "The trainer is the single point of failure for the entire CAIMIRA+EB hybrid: a regression in `collect_binary_examples` (lines 152-187) silently drops rows, a regression in `build_eb_tables` (lines 242-308) produces miscalibrated lookups, and a regression in the state_dict round-trip check (lines 387-403) defeats the very safeguard that was added specifically to prevent the April 2026 NCF silent-load-failure class of bug (per `silent-ncf-head-load-failure-via-full-module-pickle-2026-05-17.md`). The trainer's own round-trip check (lines 398-403) protects against deployment-time mismatch — but no test protects the round-trip check itself.",
      "suggested_fix": {
        "test_scaffold": "tests/test_submission/test_train.py with two tests:\n\n1. `TestTrainerSmokeRoundtrip` (marked `slow` AND `network`): synthesize a tiny offline parquet fixture (or use a real `--smoke` invocation pointed at a tmp_path output dir) and assert that (a) caimira_lite.pt, caimira_lite.meta.json, eb_tables.json all exist; (b) `torch.load(pt, weights_only=True)` succeeds; (c) `CAIMIRALite(**meta).load_state_dict(state, strict=True)` produces `missing == [] and unexpected == []`; (d) meta['subject_to_idx'] is a non-empty dict with all-int values; (e) eb_tables.json conforms to the EBLookup.from_json schema; (f) the saved CAIMIRA forward output matches the trained CAIMIRA forward output to atol=1e-6 (round-trip parity).\n\n2. `TestBuildEBTablesUnit` (no markers; pure-Python): drive `build_eb_tables` against a hand-crafted in-memory `examples` list with 3 subjects × 2 benchmarks × known label patterns, assert that (a) `global_mean`, `bench`, `subj`, `sbc`, `sb` keys are populated; (b) the `_EB_SBC_MIN_N=3` cutoff drops a 2-observation triple cell; (c) the `_EB_SUBJ_MIN_N=10` cutoff drops a subject with 5 observations; (d) Bayesian shrinkage at level 2 (line 287) shrinks a 3-observation cell of mean 1.0 toward its prior — verify the closed-form `(n*raw_p + alpha*prior_p) / (n + alpha)`; (e) `_table_clip` bounds every output to [0.05, 0.95].\n\nAlso add `TestBuildWideResponseUnit`: verify NaN-masking shape and that wide[s, i] == y for every (s, i) in the input."
      },
      "markers_recommended": "Smoke roundtrip: `slow` + `network` (downloads HF data). Unit tests on `build_eb_tables` / `_build_wide_response`: no markers (pure-Python on synthetic dicts)."
    },
    {
      "id": "CRIT-TEST-2",
      "title": "submission/model.py production path (in-vocab CAIMIRA + EB blend + Platt shift + clip) is entirely untested",
      "severity": "P1",
      "confidence": 90,
      "category": "missing-edge-case-coverage-for-error-paths",
      "anchor": {
        "file": "/Users/ankit.aggarwal/Dropbox/Stanford/CS321M/predictive-eval-competition/vasundras_torch_measure/submission/model.py",
        "lines": "220-258"
      },
      "evidence": "test_model_contract.py:25 sets `PREDICTIVE_EVAL_LOCAL_SMOKE_TEST=1` for every test and `_load_model_module` is called by every contract test. Reading model.py:136-163 shows that under LOCAL_SMOKE the encoder, CAIMIRA, EB, and META are all `None`/empty, and predict() short-circuits at line 220-221: `if _LOCAL_SMOKE: return 0.5`. As a result, lines 223-258 of model.py (the actual hybrid prediction path: assert non-None artifacts, parse_subject_name, EB.resolve_name, EB.lookup_p, SUBJECT_TO_IDX lookup, OOV fallback branch, _encode_item, CAIMIRA.caimira_logit, _sigmoid, _blend_logits, fit_platt, slope*_logit(p)+intercept, clip_for_predict) are NEVER EXECUTED in CI. Verified by grep: no test imports `caimira_lite.CAIMIRALite` + `caimira_lite.EBLookup` + a fake `META`/`caimira_lite.pt`/`eb_tables.json` to exercise the production branch. The OOV branch (line 239: `if subject_idx is None or not item_content:`) and the in-vocab branch (line 246-250: `else: item_embedding = _encode_item(...); caimira_logit = CAIMIRA.caimira_logit(...); p = _blend_logits(...)`) are both untested. The Platt application (lines 252-256) is untested at the model.py layer (only at the EBLookup layer in test_caimira_lite.py::TestEBLookupPlatt).",
      "why_it_matters": "The LOCAL_SMOKE branch is exercised by exactly 6 contract tests, all of which return the constant 0.5 and therefore test only the import + signature surface — they cannot catch a regression in the CAIMIRA+EB integration. The parent CLAUDE.md explicitly cites the silent-NCF-head-load post-mortem where THREE WEEKS OF CONSTANT-0.5 LEADERBOARD SUBMISSIONS resulted from exactly this class of coverage gap (test exercises only the smoke path; production path silently fails). The Codabench platform suppresses stdout/stderr (per the parent CLAUDE.md \"Two-tier error reporting\" pattern) so the only signal of a broken production path is a constant-0.5-shaped score on the hidden leaderboard. This is the canonical bite path that the diff's own model.py:18-27 docstring documents as the motivation for removing broad-except blocks — but without a test that actually exercises the in-vocab branch, future refactors can re-introduce the same silent failure via a different mechanism (e.g., a typo in SUBJECT_TO_IDX lookup, a wrong tensor shape passed to caimira_logit, an exception in fit_platt that goes uncaught).",
      "suggested_fix": {
        "test_scaffold": "tests/test_submission/test_model_production_path.py with class `TestModelProductionPath`:\n\nFixtures: `tmp_artifacts(tmp_path)` builds a tiny CAIMIRALite (n_subjects=3, n_items=2, embedding_dim=4, latent_dim=2), saves its state_dict to `tmp_path/caimira_lite.pt`, writes a hand-crafted `caimira_lite.meta.json` with subject_to_idx={'gpt-4': 0, 'claude-3': 1, 'llama-2-7b': 2}, and writes a hand-crafted `eb_tables.json`. Patches `_HEAD_PATH`/`_META_PATH`/`_EB_PATH` in the model module via `monkeypatch.setattr`. Uses `monkeypatch.setenv('PREDICTIVE_EVAL_LOCAL_SMOKE_TEST', '')` to force the production branch. Stubs out `SentenceTransformer` via `monkeypatch.setattr('sentence_transformers.SentenceTransformer', FakeEncoder)` where `FakeEncoder.encode` returns a deterministic `torch.zeros(4)` regardless of input.\n\nTests:\n\n1. `test_in_vocab_in_dict_path_blends_caimira_and_eb` — assert that predict({'subject_content': 'Name: gpt-4', ...}) returns a value that lies STRICTLY BETWEEN the pure-CAIMIRA p (sigmoid of caimira_logit) and the pure-EB p (eb.lookup_p), since `_BLEND_LAMBDA=0.6` is mid-range.\n\n2. `test_oov_subject_falls_back_to_eb_only` — predict({'subject_content': 'Name: TotallyUnknownModel', ...}) returns exactly `clip_for_predict(eb.lookup_p(...))`, no CAIMIRA contribution.\n\n3. `test_missing_item_content_falls_back_to_eb_only` — predict({'subject_content': 'Name: gpt-4', 'item_content': '', ...}) takes the OOV branch despite gpt-4 being in vocab.\n\n4. `test_labeled_applies_platt_shift` — predict(record, labeled=[5 examples]) returns a value DIFFERENT from predict(record, labeled=None) for a benchmark where mean_y deviates from the prior; verify the difference is consistent with the documented intercept-only Platt with ±1.5 cap.\n\n5. `test_clip_lo_hi_enforced` — feed an artificial state_dict that drives caimira_logit far positive; verify return is exactly `1 - 1e-4` (the upper clip from clip_for_predict).\n\n6. `test_assertion_fires_when_artifacts_missing_in_production_mode` — with LOCAL_SMOKE unset and `_HEAD_PATH` pointing to a nonexistent file, import of the model module raises FileNotFoundError (NOT silent-degrade to 0.5).\n\n7. `test_subject_to_idx_falls_back_to_raw_name` (model.py:235-237) — when raw_name is in SUBJECT_TO_IDX but EB.resolve_name returns a different canonical name not in the dict, the fallback `SUBJECT_TO_IDX.get(raw_name)` recovers the index.",
      "markers_recommended": "No markers (pure-Python tests; SentenceTransformer is mocked out)."
      }
    },
    {
      "id": "CRIT-TEST-3",
      "title": "submission/labeling.py stateful globals (`_seen_signatures`, `_stratum_counts`, `_candidate_count`) and reservoir-sampling logic are not exercised by any test",
      "severity": "P1",
      "confidence": 92,
      "category": "untested-branches-in-new-code",
      "anchor": {
        "file": "/Users/ankit.aggarwal/Dropbox/Stanford/CS321M/predictive-eval-competition/vasundras_torch_measure/submission/labeling.py",
        "lines": "44-47, 137-171, 207-222"
      },
      "evidence": "test_model_contract.py:106-176 contains the only tests for `acquisition_function`. The four tests cover: signature shape (line 107), single-call finite return (line 119), exception sentinel (line 139), and that a fresh single call returns score>0 (line 156). NONE of these tests cover:\n\n(a) `_update_reservoir` (labeling.py:161-171) — the documented reservoir-sampling branch at line 166-171 fires only AFTER `_MAX_SEEN=128` candidates have been seen. No test ever calls acquisition_function more than once in succession (test_acquisition_function_distinguishable_from_legitimate_floor clears globals at line 164-166 then calls ONCE).\n\n(b) `_metadata_bonus` per-stratum decay (labeling.py:131-134) — the `0.20 / (1 + count)` decay implies that repeated calls on items with the same `_stratum_key` should produce monotonically decreasing metadata bonus. Untested.\n\n(c) `_update_stratum` (labeling.py:137-140) — the `_MAX_STRATA=256` cap branch (line 139: `if key in _stratum_counts or len(_stratum_counts) < _MAX_STRATA`) is untested. A test that calls acquisition_function 300 times with 300 distinct stratum keys would catch a regression where the cap is silently dropped.\n\n(d) Determinism — given identical input twice (after global reset), does the same score return? The current tests rely only on signature membership (`signature in _seen_signatures` semantics on second call drops `_diversity_score` from 1.0 to 0.0). Not verified.\n\n(e) `_simhash` (labeling.py:82-97) — the empty-tokens branch (line 85-86: `if not toks: toks = [\"<empty>\"]`) and the `_MAX_TOKENS=256` truncation (line 66) have no dedicated coverage.\n\n(f) `_coarse_subject_bucket` (labeling.py:100-121) — the 3 branches (`org:`, `name_hash:`, `params_digits_*` fallback) are all untested.\n\n(g) The `_clamp_score` infinity-guard (labeling.py:174-177) — untested.",
      "why_it_matters": "Per `../docs/solutions/runtime-errors/labeling-py-nan-fallback-bomb-2026-05-17.md` (cited in the parent CLAUDE.md), the team has a documented prior failure where `acquisition_function` had TWO independent NaN-return paths and only ONE was caught by tests. The pattern doc explicitly warns: \"force-injection unit tests catch what code-reading misses.\" The current 4-test surface tests only the obvious paths (one call, one exception, one signature). The structurally bulletproof claim in labeling.py:14-22 (\"bounded cost\", \"NaN-immune\", \"reservoir-sampled\") REQUIRES tests that exercise the bounded/reservoir mechanism — otherwise a future refactor that breaks `_MAX_SEEN` or `_MAX_STRATA` (e.g., changes their type from int to str via copy-paste, comments out the cap branch) will silently re-introduce the pre-2026-05-21 unbounded-cost behavior on 10K-candidate rounds (per the parent CLAUDE.md \"5k → 10k sample-size increase per Ed #160\"). The Codabench platform's stdout/stderr suppression means a hung-or-timed-out acquisition step manifests only as a degraded label budget on the hidden leaderboard — the highest-stakes silent failure class in the team's known taxonomy.",
      "suggested_fix": {
        "test_scaffold": "tests/test_submission/test_labeling.py with the following classes:\n\n1. `TestSimHashAndTokens`: \n   - `test_simhash_empty_input_uses_sentinel`: `_simhash({})` does NOT raise and returns a deterministic int (the `<empty>` fallback path).\n   - `test_simhash_same_input_same_signature`: deterministic.\n   - `test_simhash_max_tokens_truncation`: input of 1000 tokens produces the same signature as the first-256-token prefix.\n\n2. `TestReservoirSampling`:\n   - `test_seen_signatures_fills_to_max_then_replaces`: clear globals; call acquisition_function 200 times with distinct inputs; assert `len(_seen_signatures) == _MAX_SEEN == 128` after, NOT 200.\n   - `test_reservoir_replacement_uses_documented_hash_slot`: after 130 calls, verify that whichever slot was replaced corresponds to the documented `_hash_u64(key + count) % count` formula by feeding a sequence where the hash output is predictable.\n   - `test_seen_signatures_does_not_grow_unbounded`: call 10_000 times; assert `len(_seen_signatures) == 128`.\n\n3. `TestStratumCounting`:\n   - `test_stratum_count_increments_on_repeat`: call acquisition_function 3 times on identical input; assert `_stratum_counts[key] == 3`.\n   - `test_metadata_bonus_decays`: call once and capture score; call N more times on the same key and capture the new score; assert the metadata-bonus component (extractable via the `_metadata_bonus` helper directly) decreased exactly as `0.20 / (1 + N)`.\n   - `test_max_strata_cap_enforced`: clear globals; feed 300 distinct stratum keys; assert `len(_stratum_counts) <= _MAX_STRATA == 256`.\n   - `test_coarse_subject_bucket_branches`: directly exercise the helper — assert org branch returns `org:openai`, name-only returns `name_hash:<int>`, params-only returns `params_digits_*`.\n\n4. `TestDeterminism`:\n   - `test_same_input_after_reset_same_score`: clear globals; call A→capture; clear globals; call A→capture; assert equal.\n\n5. `TestSentinelDistinguishability`:\n   - `test_legitimate_floor_strictly_greater_than_zero` (already partially covered at test_model_contract.py:156 — strengthen to specifically check `score >= 0.0 + tie_break_min` where `tie_break_min = (1 / 2**64) * 0.01` is the documented theoretical lower bound).\n   - `test_clamp_score_infinity_returns_zero` (force `_clamp_score(float('inf'))` directly).\n\n6. `TestNaNImmunity`:\n   - `test_input_with_none_fields_does_not_return_nan` — pass `{'benchmark': None, 'condition': None, 'subject_content': None, 'item_content': None}` → finite score >= 0.\n   - `test_input_with_torch_tensor_values_handled` — pass a non-string in a field; ensure it doesn't break `_visible_text`.",
      "markers_recommended": "No markers (all pure-Python, fast)."
      }
    },
    {
      "id": "CRIT-TEST-4",
      "title": "TestCAIMIRALiteStateDictParity is missing the load-bearing trained-weights round-trip assertion",
      "severity": "P1",
      "confidence": 88,
      "category": "tests-that-don-t-assert-behavior",
      "anchor": {
        "file": "/Users/ankit.aggarwal/Dropbox/Stanford/CS321M/predictive-eval-competition/vasundras_torch_measure/tests/test_submission/test_caimira_lite.py",
        "lines": "35-63"
      },
      "evidence": "test_caimira_lite.py:46-63 (`test_state_dict_round_trips`) constructs an upstream CAIMIRA, OVERWRITES its parameters with hand-set constants (`fill_(0.3)`, `fill_(0.1)`, `fill_(-0.05)`, `fill_(0.2)`, `fill_(0.07)`), then asserts the lite class's state_dict matches the upstream's. This tests storage parity but NOT forward parity: the `caimira_logit` arithmetic in CAIMIRALite (lines 226-243) computes `((skill - difficulty) * relevance).sum().item()`, while the upstream CAIMIRA's `predict()` (caimira.py:223-246) computes the same equation but reaches it through `compute_item_params` with frozen centering. There is no test that: (a) trains an upstream CAIMIRA on synthetic data (or just calls `_refresh_difficulty_mean`) so the buffer is non-trivial; (b) saves state_dict via `torch.save`; (c) loads into CAIMIRALite via `torch.load(..., weights_only=True)`; (d) verifies that for the SAME (subject_idx, item_embedding), upstream's `predict({'subject_idx': [i], 'item_idx': [j]})` AND lite's `caimira_logit(i, emb)` produce values consistent with `sigmoid(caimira_logit) == upstream.predict[...]` to atol=1e-6. This is exactly the silent-shape-mismatch class of bug that the parent CLAUDE.md cites the April 2026 NCF post-mortem as warning about (3 weeks of constant-0.5 submissions because state_dict load silently failed and the fallback was 0.5).",
      "why_it_matters": "The parent CLAUDE.md explicitly mandates against re-introducing this class of bug. The trainer (train.py:387-403) already has a round-trip check that runs at TRAIN time, but if the trainer's check itself is wrong (e.g., uses `strict=False` and silently accepts missing keys — see train.py:399 which DOES use `strict=False`!), the ship artifact can still mismatch the consumer. A test-time round-trip with `strict=True` AND a forward-output equivalence assertion is the only structural defense against this class. The diff's test_keys_match_upstream (line 38-44) ONLY checks set-equality of state_dict keys — it does not catch a case where keys match but shapes silently differ (e.g., `self.skill` shape `(n_subjects, latent_dim)` becoming `(latent_dim, n_subjects)` in either class). Note: `test_state_dict_round_trips` at line 57 DOES use `strict=True`, so the test does catch missing/unexpected keys — but the missing forward-equivalence assertion is the critical gap.",
      "suggested_fix": {
        "test_scaffold": "Add to TestCAIMIRALiteStateDictParity:\n\n```python\ndef test_forward_output_parity_after_save_load(self, tmp_path):\n    \"\"\"upstream.predict() output == sigmoid(lite.caimira_logit()) after save+load.\"\"\"\n    torch.manual_seed(0)\n    n_s, n_i, d, k = 5, 7, 16, 3\n    upstream = CAIMIRA(n_subjects=n_s, n_items=n_i, embedding_dim=d, latent_dim=k)\n    embeddings = torch.randn(n_i, d)\n    responses = torch.bernoulli(torch.full((n_s, n_i), 0.5))\n    upstream.fit(responses, embeddings, max_epochs=5, verbose=False)\n\n    pt_path = tmp_path / 'caimira_lite.pt'\n    torch.save(upstream.state_dict(), pt_path)\n\n    lite = CAIMIRALite(n_subjects=n_s, n_items=n_i, embedding_dim=d, latent_dim=k)\n    state = torch.load(pt_path, map_location='cpu', weights_only=True)\n    lite.load_state_dict(state, strict=True)\n    lite.eval()\n    upstream.eval()\n    upstream.set_embeddings(embeddings)\n\n    # Verify every (subject, item) pair matches.\n    for i in range(n_s):\n        for j in range(n_i):\n            upstream_p = upstream.predict(\n                {'subject_idx': torch.tensor([i]), 'item_idx': torch.tensor([j])}\n            ).item()\n            lite_logit = lite.caimira_logit(subject_idx=i, item_embedding=embeddings[j])\n            lite_p = 1.0 / (1.0 + math.exp(-lite_logit))\n            assert abs(upstream_p - lite_p) < 1e-5, (\n                f'Forward mismatch at ({i}, {j}): upstream={upstream_p}, lite={lite_p}'\n            )\n\ndef test_buffer_difficulty_mean_survives_save_load(self, tmp_path):\n    \"\"\"_difficulty_mean is a buffer, not a parameter — must survive torch.save round-trip.\"\"\"\n    n_s, n_i, d, k = 3, 5, 8, 2\n    upstream = CAIMIRA(n_subjects=n_s, n_items=n_i, embedding_dim=d, latent_dim=k)\n    upstream.set_embeddings(torch.randn(n_i, d))\n    upstream._refresh_difficulty_mean()\n    nonzero_mean = upstream._difficulty_mean.clone()\n    assert torch.any(nonzero_mean != 0)  # sanity: nontrivial buffer\n\n    pt = tmp_path / 'm.pt'\n    torch.save(upstream.state_dict(), pt)\n    lite = CAIMIRALite(n_subjects=n_s, n_items=n_i, embedding_dim=d, latent_dim=k)\n    state = torch.load(pt, map_location='cpu', weights_only=True)\n    lite.load_state_dict(state, strict=True)\n    assert torch.allclose(lite._difficulty_mean, nonzero_mean, atol=1e-6)\n```\n\nAlso strengthen the trainer-side round-trip in test_caimira_lite.py: add a test that the trainer's `strict=False` at train.py:399 still raises SystemExit when missing/unexpected keys are produced (currently the trainer-side guard at line 400-403 is what catches the mismatch, but no test verifies that guard fires)."
      },
      "markers_recommended": "No markers (CPU-only, ~1s)."
    },
    {
      "id": "CRIT-TEST-5",
      "title": "test_predict_returns_native_float and the rest of TestModelContract assert the LOCAL_SMOKE return is `[0,1]` but never assert the documented sentinel value 0.5",
      "severity": "P2",
      "confidence": 80,
      "category": "tests-that-don-t-assert-behavior",
      "anchor": {
        "file": "/Users/ankit.aggarwal/Dropbox/Stanford/CS321M/predictive-eval-competition/vasundras_torch_measure/tests/test_submission/test_model_contract.py",
        "lines": "52-103"
      },
      "evidence": "model.py:220-221 documents and implements `if _LOCAL_SMOKE: return 0.5`. Every test in TestModelContract sets LOCAL_SMOKE=1 (via `_load_model_module` at line 25) and asserts `isinstance(out, float)`, `not isinstance(out, bool)`, `math.isfinite(out)`, `0.0 <= out <= 1.0`. NONE assert `out == 0.5`. As a result, if a future refactor changes the LOCAL_SMOKE sentinel from `0.5` to e.g. `0.604` (the constant from the D-9 sub-gate (a) note in submission/README.md), or if the contract path silently degrades to a different constant, the tests still pass. This is a textbook \"asserts truthiness instead of specific values\" pattern from the testing reviewer charter.",
      "why_it_matters": "The 0.5 sentinel is contractually significant: it is the exact return that the kit's `tools/run_smoke_test.py` and `tools/check_submission_zip.py` use to verify the import + signature surface, and the constant-0.5 output is the canonical signature of the silent-failure class the parent CLAUDE.md documents. A test that doesn't pin the sentinel value cannot catch a refactor that breaks the LOCAL_SMOKE contract.",
      "suggested_fix": {
        "test_scaffold": "Add to TestModelContract:\n\n```python\ndef test_predict_returns_documented_smoke_sentinel(self):\n    \"\"\"LOCAL_SMOKE branch returns exactly 0.5 per model.py:220-221.\"\"\"\n    mod = _load_model_module()\n    sample_input = {\n        'benchmark': 'mmlupro', 'condition': 'cot',\n        'subject_content': 'Name: gpt-4', 'item_content': 'Q',\n    }\n    out = mod.predict(sample_input, labeled=None)\n    assert out == 0.5, f'LOCAL_SMOKE sentinel changed: got {out}'\n```\n\nNote: this is a P2 because the existing tests DO cover signature + finiteness; this is a hardening, not a coverage gap of the production path (which is CRIT-TEST-2)."
      },
      "markers_recommended": "No markers."
    },
    {
      "id": "CRIT-TEST-6",
      "title": "Zero pytest markers in any of the 5 new test files — slow / network / gpu markers are required by project convention and CI filters",
      "severity": "P2",
      "confidence": 95,
      "category": "missing-edge-case-coverage-for-error-paths",
      "anchor": {
        "file": "/Users/ankit.aggarwal/Dropbox/Stanford/CS321M/predictive-eval-competition/vasundras_torch_measure/tests/test_models/test_caimira.py",
        "lines": "1-178"
      },
      "evidence": "Grep `pytest\\.mark|@.*slow|@.*network|@.*gpu` across all 5 new test files returns ZERO matches. pyproject.toml:89-93 declares the three markers as the project's convention; CONTRIBUTING.md (per primer line 211) mandates them \"for expensive cases\". `.github/workflows/test.yml:37` runs `-m \"not slow and not gpu\"`, and `slow-tests.yml:39` runs `-m \"not gpu\"` weekly. Without any markers:\n- `test_fit_reduces_loss` (test_caimira.py:151) runs a 50-epoch CAIMIRA training loop on every PR; should be `slow`.\n- `test_strong_signal_recovers_alpha_sign` (test_llm_judge_irt.py:210) runs a 400-row scipy.optimize.minimize Nelder-Mead simplex; depends on scipy via `pytest.importorskip`; should be `slow`.\n- If `test_train.py` is added per CRIT-TEST-1, the smoke roundtrip test downloads from HF — must be marked `network` (or it will hang the PR CI which can't reach HuggingFace under sandbox).\n- `test_model_contract.py` does NOT download (LOCAL_SMOKE skips the encoder load), but if the suggested CRIT-TEST-2 production-path test runs WITHOUT mocking the SentenceTransformer, it DOES download — must be `network`.",
      "why_it_matters": "Without markers, slow tests bloat every PR run and `slow-tests.yml`'s weekly cadence delivers no additional coverage (everything already runs). When a future test that DOES need network access (per CRIT-TEST-1) is added without `network`, it will fail the sandbox-free CI in `test.yml` and the team will resort to skipping or weakening — a known regression pattern in the team's history (per the parent CLAUDE.md's emphasis on never weakening tests under deadline pressure).",
      "suggested_fix": {
        "test_scaffold": "Add markers:\n\n```python\n# test_caimira.py:151 — runs 50 epochs of CAIMIRA training\n@pytest.mark.slow\ndef test_fit_reduces_loss(self, small_response_matrix):\n    ...\n\n# test_caimira.py:107 — runs 5 epochs of CAIMIRA training (borderline; keep unmarked OK if <2s)\n# test_caimira.py:127 — manual response equation; pure forward; no marker.\n\n# test_llm_judge_irt.py:196, 210, 232 — scipy.optimize.minimize 100/400 rows each\n@pytest.mark.slow\nclass TestFitAlphaBeta:\n    ...\n```\n\nFor any new test that touches HuggingFace or SentenceTransformer loads (e.g., the CRIT-TEST-1 trainer roundtrip), add `@pytest.mark.network` and document the requirement in a module-level docstring."
      },
      "markers_recommended": "n/a (this finding IS about markers)"
    },
    {
      "id": "CRIT-TEST-7",
      "title": "test_fit_reduces_loss is a weak assertion that any minimal optimizer pass would satisfy",
      "severity": "P3",
      "confidence": 70,
      "category": "tests-that-don-t-assert-behavior",
      "anchor": {
        "file": "/Users/ankit.aggarwal/Dropbox/Stanford/CS321M/predictive-eval-competition/vasundras_torch_measure/tests/test_models/test_caimira.py",
        "lines": "151-157"
      },
      "evidence": "Line 157: `assert history[\"losses\"][-1] < history[\"losses\"][0]`. This passes as long as the final loss is even ε less than the initial loss. A trivial Adam-on-random-init walk reduces loss by chance on the first epoch on most synthetic data, so this assertion would pass even if the fit logic were silently broken (e.g., gradient flowing only to one parameter group, or the L1 regularization completely dominating signal). A meaningful version asserts (a) a non-trivial percentage reduction (e.g., `losses[-1] < 0.5 * losses[0]`), OR (b) parameter recovery against synthetic ground truth (recover subject ranking by skill within Kendall-τ tolerance), OR (c) monotonicity for the last K epochs (`np.diff(losses[-10:]).max() < 1e-3`).",
      "why_it_matters": "The fit logic at caimira.py:248-344 has multiple non-trivial branches (the L1 reg term at line 325, the dynamic-vs-frozen centering switching in compute_item_params:203-212, the buffer refresh at line 343, the `caimira_loss` closure at line 319) — a weak \"loss went down\" assertion provides false confidence that they're all wired up correctly. Note: this is P3 not P1 because `test_predict_matches_manual_caimira_equation` (line 127) does provide a forward-equation correctness check that catches most wiring errors; the weak fit-loss test would only catch a regression in the training-loop wiring that the forward check missed.",
      "suggested_fix": {
        "test_scaffold": "Strengthen test_fit_reduces_loss:\n\n```python\ndef test_fit_meaningfully_reduces_loss(self, small_response_matrix):\n    torch.manual_seed(0)\n    n_subjects, n_items = small_response_matrix.shape\n    embeddings = torch.randn(n_items, 16)\n    model = CAIMIRA(n_subjects=n_subjects, n_items=n_items, embedding_dim=16, latent_dim=3)\n    history = model.fit(small_response_matrix, embeddings, max_epochs=50, verbose=False)\n    initial = history['losses'][0]\n    final = history['losses'][-1]\n    # Meaningful improvement: at least 20% reduction\n    assert final < 0.8 * initial, f'fit barely reduced loss: {initial:.4f} → {final:.4f}'\n    # Monotone in the last 5 epochs (no late-stage divergence)\n    last_5 = history['losses'][-5:]\n    assert max(np.diff(last_5)) < 1e-2\n\ndef test_fit_recovers_subject_skill_ranking(self, seed):\n    \"\"\"On data generated from a known skill ordering, fit should recover the ranking.\"\"\"\n    torch.manual_seed(0)\n    n_s, n_i, k = 8, 50, 2\n    true_skill = torch.linspace(-2, 2, n_s).unsqueeze(1).repeat(1, k)\n    embeddings = torch.randn(n_i, 16)\n    # Generate from a CAIMIRA-shaped model with the true skill\n    # (fixture-style; would mirror small_response_matrix but with known structure)\n    ...\n    model = CAIMIRA(n_subjects=n_s, n_items=n_i, embedding_dim=16, latent_dim=k)\n    model.fit(responses, embeddings, max_epochs=200, verbose=False)\n    learned_rank = model.skill.sum(dim=1).argsort()\n    true_rank = true_skill.sum(dim=1).argsort()\n    # Kendall tau or simply spearman-style overlap\n    overlap = (learned_rank == true_rank).float().mean().item()\n    assert overlap > 0.6, f'subject ranking recovery poor: {overlap:.2f}'\n```"
      },
      "markers_recommended": "`slow` (both involve 50+ / 200+ epoch training loops)."
    },
    {
      "id": "CRIT-TEST-8",
      "title": "TestEBLookup omits edge cases that EBLookup.lookup_p must handle: empty sbc/sb/subj/bench dicts, condition-`none` cascade, mean_y boundary in fit_platt",
      "severity": "P2",
      "confidence": 75,
      "category": "missing-edge-case-coverage-for-error-paths",
      "anchor": {
        "file": "/Users/ankit.aggarwal/Dropbox/Stanford/CS321M/predictive-eval-competition/vasundras_torch_measure/tests/test_submission/test_caimira_lite.py",
        "lines": "141-241"
      },
      "evidence": "TestEBLookup tests the 6 fallback levels with a populated `lookup` fixture. NOT covered:\n\n(a) **All-empty tables**: every level falls through to `global_mean`. EBLookup({}, {}, {}, {}, 0.5).lookup_p('x', 'y', 'z') should return 0.5. Untested. (test_cold_start_lookup.py:337 does test the upstream's empty-input path, but not the submission's EBLookup version which has different code at caimira_lite.py:316-344.)\n\n(b) **The `key3_none` cascade** (caimira_lite.py:325-327): when condition='xyz' is unknown but `sbc['gpt-4||mmlupro||none']` exists, the lookup should fall back to the `none` condition entry within level 1 (NOT drop to level 2). Untested in the submission's EBLookup; test_cold_start_lookup.py:107 covers the upstream's analogous path but with a different code path.\n\n(c) **Case-insensitive fallbacks** at lines 323, 332, 335-336 of caimira_lite.py — `self._sbc_ci`, `self._sb_ci`, `self._subj_ci`, `self._bench_ci` — none of these CI views are exercised by TestEBLookup's tests.\n\n(d) **`fit_platt` ALL-ZEROS labels boundary** (caimira_lite.py:377-382): the test at test_caimira_lite.py:185 covers mean_y==1.0; mean_y==0.0 is asymmetric in floating-point arithmetic (the `mean_y <= 0` branch fires) and is NOT tested.\n\n(e) **`fit_platt` single-pair guard** (caimira_lite.py:372-373: `if len(pairs) < 2: continue`): one labeled example per benchmark should produce no Platt entry. test_cold_start_lookup.py:227 tests this for the upstream; the submission's EBLookup is NOT separately tested.\n\n(f) **`predict` with `labeled=[]` (empty list)**: per the kit contract (model.py:210-213), labeled may be `[]` after a category exhausts. Untested.",
      "why_it_matters": "These are all branches in caimira_lite.py:316-408 — the actual production code at runtime. The test_cold_start_lookup.py tests partially mirror them but on a DIFFERENT class (ColdStartLookupPredictor), so a divergence between the two implementations (e.g., a typo in caimira_lite.py's case-insensitive view construction) goes undetected. The team explicitly documents at caimira_lite.py:5-30 that this is a STANDALONE PORT and must mirror the upstream byte-for-byte; tests should verify the port, not assume it.",
      "suggested_fix": {
        "test_scaffold": "Add to TestEBLookup:\n\n```python\ndef test_all_empty_tables_returns_global(self):\n    lookup = EBLookup(sbc={}, sb={}, subj={}, bench={}, global_mean=0.42)\n    assert lookup.lookup_p('x', 'y', 'z') == 0.42\n\ndef test_condition_none_cascade_within_level1(self):\n    lookup = EBLookup(\n        sbc={'gpt-4||mmlupro||none': 0.88},\n        sb={}, subj={}, bench={}, global_mean=0.5,\n    )\n    assert lookup.lookup_p('gpt-4', 'mmlupro', 'unknown_cond') == 0.88\n\ndef test_case_insensitive_subject_fallback(self):\n    lookup = EBLookup(\n        sbc={}, sb={}, subj={'GPT-4': 0.77},\n        bench={'mmlupro': 0.5}, global_mean=0.5,\n    )\n    # Lower-case subj should hit the _subj_ci view\n    p = lookup.lookup_p('gpt-4', 'NewBench', 'cot')\n    assert p == 0.77\n```\n\nAdd to TestEBLookupPlatt:\n\n```python\ndef test_fit_platt_skips_when_all_zero_labels(self):\n    lookup = EBLookup(sbc={}, sb={}, subj={}, bench={'b1': 0.3}, global_mean=0.5)\n    labeled = [{'subject_content': 'Name: m', 'benchmark': 'b1', 'condition': 'x', 'label': 0.0}] * 5\n    lookup.fit_platt(labeled)\n    assert 'b1' not in lookup._platt  # mean_y == 0 → skipped\n\ndef test_fit_platt_skips_singletons(self):\n    lookup = EBLookup(sbc={}, sb={}, subj={}, bench={'b1': 0.3}, global_mean=0.5)\n    labeled = [{'subject_content': 'Name: m', 'benchmark': 'b1', 'condition': 'x', 'label': 1.0}]\n    lookup.fit_platt(labeled)\n    assert 'b1' not in lookup._platt\n\ndef test_predict_with_empty_labeled_list(self):\n    lookup = EBLookup(sbc={}, sb={}, subj={}, bench={'b1': 0.3}, global_mean=0.5)\n    p = lookup.predict('Name: m', 'b1', 'x', labeled=[])\n    assert abs(p - 0.3) < 1e-6  # bench-only level 4\n```"
      },
      "markers_recommended": "No markers."
    },
    {
      "id": "CRIT-TEST-9",
      "title": "submission/build_zip.sh has zero test coverage — multiple branches (--force, missing-required-file, allowlist mismatch, single-.pt check, nested-paths sanity) can silently break the ZIP",
      "severity": "P2",
      "confidence": 85,
      "category": "behavioral-changes-with-no-test-additions",
      "anchor": {
        "file": "/Users/ankit.aggarwal/Dropbox/Stanford/CS321M/predictive-eval-competition/vasundras_torch_measure/submission/build_zip.sh",
        "lines": "1-139"
      },
      "evidence": "139 LoC of bash strict-mode shell with multiple branches at lines 34-48 (argument parsing), 65-80 (missing-file refusal), 83-86 (refuse-without-force), 91-99 (single-.pt safety check), 110-116 (nested-path sanity), 118-127 (allowlist-vs-actual comparison). No corresponding shell test. The script is the ONLY way the team produces a ZIP (per submission/README.md:1-30, train.py:436), and the kit's pre-validator (referenced at build_zip.sh:9-14) rejects ZIPs that violate any of these rules — meaning a silent breakage of build_zip.sh produces a `[PAIEC-PREDICT-002]` from Codabench, which the parent CLAUDE.md \"Two-tier error reporting\" pattern doc warns about as consuming a daily-quota submission slot for under-diagnosed failures.",
      "why_it_matters": "Shell scripts are notoriously easy to break with quote-escape issues. The lines 119-122 in particular do a fragile `echo "${zip_files}" | sort` versus `printf '%s\\n' "${REQUIRED_FILES[@]}" | sort` comparison — if a future change adds a file with a space in its name, this comparison would silently allow a mismatch. The parent CLAUDE.md cites `bash-retry-loop-silent-success-pipefail-without-errexit-2026-05-21.md` as a documented prior bash-strict-mode gotcha; build_zip.sh DOES have `set -euo pipefail` (line 26), so the most obvious gotcha is covered — but the script-level test would be the structural defense against ALL of these classes simultaneously.",
      "suggested_fix": {
        "test_scaffold": "tests/test_submission/test_build_zip.py with class `TestBuildZipShell` using subprocess:\n\n```python\nimport subprocess\nfrom pathlib import Path\nimport pytest\nimport zipfile\n\nREPO_ROOT = Path(__file__).resolve().parents[2]\nBUILD_SCRIPT = REPO_ROOT / 'submission' / 'build_zip.sh'\n\n\n@pytest.fixture\ndef fake_submission(tmp_path):\n    \"\"\"Mirror the layout of submission/ with stub artifacts.\"\"\"\n    sub = tmp_path / 'submission'\n    sub.mkdir()\n    for f in ['model.py', 'labeling.py', 'caimira_lite.py']:\n        (sub / f).write_text('# stub\\n')\n    (sub / 'models.txt').write_text('sentence-transformers/all-mpnet-base-v2\\n')\n    (sub / 'caimira_lite.pt').write_bytes(b'\\x00' * 100)\n    (sub / 'caimira_lite.meta.json').write_text('{}\\n')\n    (sub / 'eb_tables.json').write_text('{}\\n')\n    # Copy build_zip.sh into the fake submission dir so SCRIPT_DIR resolves correctly\n    import shutil\n    shutil.copy(BUILD_SCRIPT, sub / 'build_zip.sh')\n    return sub\n\n\nclass TestBuildZipShell:\n    def test_happy_path_produces_flat_zip(self, fake_submission, tmp_path):\n        out = tmp_path / 'out.zip'\n        result = subprocess.run(\n            ['bash', str(fake_submission / 'build_zip.sh'), str(out)],\n            capture_output=True, text=True,\n        )\n        assert result.returncode == 0, result.stderr\n        with zipfile.ZipFile(out) as z:\n            names = z.namelist()\n        # No nested paths\n        assert all('/' not in n for n in names)\n        # Exactly the 7 allowlisted files\n        assert set(names) == {\n            'model.py', 'labeling.py', 'models.txt', 'caimira_lite.py',\n            'caimira_lite.pt', 'caimira_lite.meta.json', 'eb_tables.json',\n        }\n\n    def test_refuses_when_required_file_missing(self, fake_submission, tmp_path):\n        (fake_submission / 'caimira_lite.pt').unlink()\n        result = subprocess.run(\n            ['bash', str(fake_submission / 'build_zip.sh'), str(tmp_path / 'out.zip')],\n            capture_output=True, text=True,\n        )\n        assert result.returncode != 0\n        assert 'missing required files' in result.stderr\n\n    def test_refuses_overwrite_without_force(self, fake_submission, tmp_path):\n        out = tmp_path / 'existing.zip'\n        out.write_bytes(b'old content')\n        result = subprocess.run(\n            ['bash', str(fake_submission / 'build_zip.sh'), str(out)],\n            capture_output=True, text=True,\n        )\n        assert result.returncode != 0\n        assert 'refusing to overwrite' in result.stderr\n        # Original file untouched\n        assert out.read_bytes() == b'old content'\n\n    def test_force_flag_allows_overwrite(self, fake_submission, tmp_path):\n        out = tmp_path / 'existing.zip'\n        out.write_bytes(b'old content')\n        result = subprocess.run(\n            ['bash', str(fake_submission / 'build_zip.sh'), '--force', str(out)],\n            capture_output=True, text=True,\n        )\n        assert result.returncode == 0\n        with zipfile.ZipFile(out) as z:\n            assert 'model.py' in z.namelist()\n\n    def test_help_flag_returns_zero_and_prints_usage(self, fake_submission):\n        result = subprocess.run(\n            ['bash', str(fake_submission / 'build_zip.sh'), '--help'],\n            capture_output=True, text=True,\n        )\n        assert result.returncode == 0\n        assert 'Build a flat Codabench-ready submission ZIP' in result.stdout\n```"
      },
      "markers_recommended": "No markers (fast; just spawns subprocesses)."
    },
    {
      "id": "CRIT-TEST-10",
      "title": "test_calibration_does_not_contaminate_other_benchmarks: the fixture only includes 4 distinct labels with mean 0.2 — the test passes for the wrong reason (no cybench Platt entry, not a non-contamination guarantee)",
      "severity": "P3",
      "confidence": 65,
      "category": "tests-that-don-t-assert-behavior",
      "anchor": {
        "file": "/Users/ankit.aggarwal/Dropbox/Stanford/CS321M/predictive-eval-competition/vasundras_torch_measure/tests/test_models/test_cold_start_lookup.py",
        "lines": "210-225"
      },
      "evidence": "test_cold_start_lookup.py:213-225: labeled list has 5 entries all for benchmark 'mmlupro'. The test asserts that `cybench` prediction is unchanged after the labeled list is passed. The reason this passes is that `calibrate` (cold_start_lookup.py:394-446) builds `by_bench` from the input, populates only `_platt['mmlupro']`, and the consumer at line 375-377 checks `if benchmark in self._platt:` so a cybench predict bypasses calibration. The test as written would still pass even if calibrate had a global-fallback contamination bug (e.g., if line 416 `self._platt.clear()` were removed, second-call state would leak between benchmarks — but the test only does ONE calibrate). A stronger version: (1) calibrate(labeled_mmlupro), capture cybench prediction; (2) calibrate(labeled_cybench), capture cybench prediction again (verify NOW changes); (3) calibrate(labeled_mmlupro) AGAIN, verify cybench prediction is back to the cold value (no stale `_platt['cybench']` leakage).",
      "why_it_matters": "P3 because the no-contamination property IS verified, but only at the level of \"a single calibrate call doesn't add a global key.\" The repeated-calibrate scenario (which is the actual production path: predict() calls fit_platt every round per model.py:252-253) is not separately tested.",
      "suggested_fix": {
        "test_scaffold": "Add:\n\n```python\ndef test_calibrate_per_benchmark_state_does_not_leak_across_rounds(\n    self, predictor: ColdStartLookupPredictor,\n) -> None:\n    mmlu_labeled = [\n        {'benchmark': 'mmlupro', 'subject_content': 'Name: gpt-4', 'label': 0}\n        for _ in range(5)\n    ]\n    cy_labeled = [\n        {'benchmark': 'cybench', 'subject_content': 'Name: gpt-4', 'label': 1}\n        for _ in range(5)\n    ]\n    p_cold = predictor.predict(_make_record('cybench', 'none', 'gpt-4'))\n\n    p1 = predictor.predict(_make_record('cybench', 'none', 'gpt-4'), mmlu_labeled)\n    p2 = predictor.predict(_make_record('cybench', 'none', 'gpt-4'), cy_labeled)\n    p3 = predictor.predict(_make_record('cybench', 'none', 'gpt-4'), mmlu_labeled)\n\n    assert abs(p1 - p_cold) < 1e-6  # mmlu doesn't affect cybench\n    assert abs(p2 - p_cold) > 0.01  # cybench labeled DOES affect cybench\n    assert abs(p3 - p_cold) < 1e-6  # back to cold (no stale cybench Platt)\n```"
      },
      "markers_recommended": "No markers."
    },
    {
      "id": "CRIT-TEST-11",
      "title": "tutorials/predictive_evaluation_challenge.ipynb (622 LoC of executable code with embedded outputs) is not re-executed in CI",
      "severity": "P3",
      "confidence": 75,
      "category": "behavioral-changes-with-no-test-additions",
      "anchor": {
        "file": "/Users/ankit.aggarwal/Dropbox/Stanford/CS321M/predictive-eval-competition/vasundras_torch_measure/tutorials/predictive_evaluation_challenge.ipynb",
        "lines": "1-622"
      },
      "evidence": "Per the primer (line 60), the diff adds a 622-line notebook with executed outputs. No CI workflow re-executes notebooks (`grep -rn nbformat\\|jupyter\\|nbconvert .github/workflows` would return empty — none of test.yml, slow-tests.yml, lint.yml, publish-test.yml, publish.yml mention notebooks). The notebook references `torch_measure.models.ColdStartLookupPredictor`, `torch_measure.models.LLMJudgeIRT`, and (per the diff's intent) the `CAIMIRA` class. If any of these public APIs change shape (e.g., a constructor argument is renamed, a method is moved private), the notebook's executed outputs drift from reality without CI noticing — the canonical class of \"tutorial silently wrong\" doc rot.",
      "why_it_matters": "P3 not P1 because notebook tests are expensive and breakage manifests as a UX issue (a user follows the tutorial and it errors), not a runtime regression on production. But the diff is upstream-eligible (per `_PRIMER.md`'s \"upstream PR\" framing in the routing/fork section), and the upstream `aims-foundations/torch_measure` may have stricter doc-execution conventions. At minimum: add a `make notebook-test` target or a `pytest --nbval-lax tutorials/` CI step under `slow-tests.yml`.",
      "suggested_fix": {
        "test_scaffold": "Option A (cheap): add to `.pre-commit-config.yaml` a hook that strips notebook outputs (`nbstripout`) and rejects committed notebooks with outputs to avoid the drift-vs-reality problem.\n\nOption B (CI re-execution): in `.github/workflows/slow-tests.yml`, after the pytest step, add:\n\n```yaml\n      - name: Validate tutorial notebook execution\n        run: |\n          pip install nbval\n          pytest --nbval-lax tutorials/predictive_evaluation_challenge.ipynb -v\n```\n\nMark with `slow` (and `network` if the notebook downloads from HF)."
      },
      "markers_recommended": "`slow` + `network`."
    }
  ],
  "residual_risks": [
    "EB-table `_table_clip` and `_logit` numerical safety on a SINGLE-labeled-example benchmark — caimira_lite.py:372-373's `if len(pairs) < 2: continue` is the documented guard but only test_cold_start_lookup.py:227 tests the upstream's analogous guard. If a future refactor changes the threshold to `< 1` (an off-by-one), no test in this diff catches it on the submission side.",
    "The CAIMIRA paper-faithfulness claims (bias asymmetry at caimira.py:131-132; zero-centering protocol at caimira.py:79-87; default `latent_dim=5`) are tested as STRUCTURE (test_difficulty_head_is_linear_and_biasless, test_relevance_head_is_linear_with_bias) but not as MATHEMATICAL PROPERTY against a reference implementation. If the paper's equation in test_predict_matches_manual_caimira_equation (line 127) differs from the paper's actual equation, the test is self-consistent but wrong.",
    "The trainer at train.py:387-403 uses `strict=False` on the round-trip load (line 399). The diff's test_state_dict_round_trips uses `strict=True` (test_caimira_lite.py:57). If the trainer's `strict=False` silently accepts a missing key in the future, the trainer-side check passes but the ship-side production model has a missing key — silently re-introducing the silent-NCF-head-load class of bug. CRIT-TEST-4's suggested test covers the test-time side; the trainer-side `strict=False` is itself the residual risk."
  ],
  "testing_gaps": [
    "No fuzz / property-based testing on the lookup_p hierarchy (the 6 cases × 2 case-insensitive variants × 4 subject-name resolution paths is a large state space; a Hypothesis-style generator would be a structural improvement but is out of scope for this PR).",
    "No integration test that exercises the full `submission/` flow end-to-end: build artifacts via train.py --smoke → bash build_zip.sh → check_submission_zip.py → run_smoke_test.py. The parent repo (`predictive-eval-competition`) has `scripts/check_submission.sh` and `scripts/smoke_test_submission.sh` that exercise this, but the fork's tests don't invoke them. A composite shell test (subprocess-driven, marked `slow + network`) would close the gap.",
    "test_caimira.py uses class-grouping (1 class with 17 tests) but the other 4 test files use sub-class grouping. P3 readability issue; not a coverage gap.",
    "No coverage threshold enforced in `.github/workflows/test.yml:37` — `--cov-report=xml` produces a report but no `--cov-fail-under` floor; a regression that drops coverage by 20% goes silent."
  ]
}
```

## File paths relevant to this review

- `/Users/ankit.aggarwal/Dropbox/Stanford/CS321M/predictive-eval-competition/vasundras_torch_measure/submission/train.py` (untested, 458 LoC)
- `/Users/ankit.aggarwal/Dropbox/Stanford/CS321M/predictive-eval-competition/vasundras_torch_measure/submission/model.py` (production path untested; only LOCAL_SMOKE branch exercised)
- `/Users/ankit.aggarwal/Dropbox/Stanford/CS321M/predictive-eval-competition/vasundras_torch_measure/submission/labeling.py` (stateful globals + reservoir + stratum + simhash internals all untested)
- `/Users/ankit.aggarwal/Dropbox/Stanford/CS321M/predictive-eval-competition/vasundras_torch_measure/submission/build_zip.sh` (no test coverage; 9 branches at risk)
- `/Users/ankit.aggarwal/Dropbox/Stanford/CS321M/predictive-eval-competition/vasundras_torch_measure/submission/caimira_lite.py` (state-dict storage parity tested; forward-output parity NOT tested)
- `/Users/ankit.aggarwal/Dropbox/Stanford/CS321M/predictive-eval-competition/vasundras_torch_measure/tests/test_submission/test_model_contract.py` (LOCAL_SMOKE branch only; missing sentinel value pin)
- `/Users/ankit.aggarwal/Dropbox/Stanford/CS321M/predictive-eval-competition/vasundras_torch_measure/tests/test_submission/test_caimira_lite.py` (key parity tested; trained-weight round-trip parity NOT tested)
- `/Users/ankit.aggarwal/Dropbox/Stanford/CS321M/predictive-eval-competition/vasundras_torch_measure/tests/test_models/test_caimira.py` (loss-reduction assertion is weak; no markers)
- `/Users/ankit.aggarwal/Dropbox/Stanford/CS321M/predictive-eval-competition/vasundras_torch_measure/tests/test_models/test_cold_start_lookup.py` (no-contamination test passes for the wrong reason; repeated-calibrate scenario uncovered)
- `/Users/ankit.aggarwal/Dropbox/Stanford/CS321M/predictive-eval-competition/vasundras_torch_measure/tests/test_models/test_llm_judge_irt.py` (slow tests not marked)
- `/Users/ankit.aggarwal/Dropbox/Stanford/CS321M/predictive-eval-competition/vasundras_torch_measure/pyproject.toml` (declares `slow`/`gpu`/`network` markers at lines 89-93; none used in new tests)
- `/Users/ankit.aggarwal/Dropbox/Stanford/CS321M/predictive-eval-competition/vasundras_torch_measure/.github/workflows/test.yml` (filters `-m "not slow and not gpu"` at line 37; affected by missing markers)
- `/Users/ankit.aggarwal/Dropbox/Stanford/CS321M/predictive-eval-competition/vasundras_torch_measure/tutorials/predictive_evaluation_challenge.ipynb` (622 LoC; no CI re-execution)
